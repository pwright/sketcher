title: iPerf
subtitle: Perform real-time network throughput measurements while using iPerf3
overview: "This tutorial demonstrates how to perform real-time network throughput\
  \ measurements across Kubernetes \nusing the iperf3 tool.\nIn this tutorial you:\n\
  * deploy iperf3 in two separate clusters\n* run iperf3 client test instances\n"
prerequisites: "* The `kubectl` command-line tool, version 1.15 or later\n([installation\
  \ guide][install-kubectl])\n\n* Access to two clusters to observe performance. \n\
  As an example, the two clusters might consist of:\n\n* A private cloud cluster running\
  \ on your local machine (**east**)\n* A public cloud cluster running in a public\
  \ cloud provider (**west**)\n"
sites:
  west:
    platform: kubernetes
    namespace: west
    env:
      KUBECONFIG: ~/.kube/config-west
  east:
    platform: kubernetes
    namespace: east
    env:
      KUBECONFIG: ~/.kube/config-east
steps:
- title: Access your Kubernetes clusters
  numbered: true
  preamble: 'Skupper is designed for use with multiple Kubernetes clusters.

    The `skupper` and `kubectl` commands use your

    [kubeconfig][kubeconfig] and current context to select the cluster

    and namespace where they operate.


    [kubeconfig]: https://kubernetes.io/docs/concepts/configuration/organize-cluster-access-kubeconfig/


    This example uses multiple cluster contexts at once. The

    `KUBECONFIG` environment variable tells `skupper` and `kubectl`

    which kubeconfig to use.


    For each cluster, open a new terminal window.  In each terminal,

    set the `KUBECONFIG` environment variable to a different path and

    log in to your cluster.'
  postamble: '**Note:** The login procedure varies by provider.'
  commands:
    west:
    - run: export KUBECONFIG=~/.kube/config-west
    - run: <provider-specific login command>
      apply: readme
    east:
    - run: export KUBECONFIG=~/.kube/config-east
    - run: <provider-specific login command>
      apply: readme
- title: Create your Kubernetes namespaces
  numbered: true
  preamble: 'The example application has different components deployed to

    different Kubernetes namespaces.  To set up our example, we need

    to create the namespaces.


    For each cluster, use `kubectl create namespace` and `kubectl

    config set-context` to create the namespace you wish to use and

    set the namespace on your current context.'
  commands:
    west:
    - run: kubectl create namespace west
      apply: readme
    - run: kubectl create namespace west --dry-run=client -o yaml | kubectl apply
        -f -
      apply: test
    - run: kubectl config set-context --current --namespace west
    east:
    - run: kubectl create namespace east
      apply: readme
    - run: kubectl create namespace east --dry-run=client -o yaml | kubectl apply
        -f -
      apply: test
    - run: kubectl config set-context --current --namespace east
- title: Install Skupper on your Kubernetes clusters
  numbered: true
  preamble: 'Using Skupper on Kubernetes requires the installation of the

    Skupper custom resource definitions (CRDs) and the Skupper

    controller.


    For each cluster, use `kubectl apply` with the Skupper

    installation YAML to install the CRDs and controller.'
  commands:
    west:
    - run: kubectl apply -f https://skupper.io/v2/install.yaml
      apply: readme
    - run: curl https://skupper.io/v2/install.yaml | sed s/Always/IfNotPresent/g |
        kubectl apply -f -
      apply: test
    east:
    - run: kubectl apply -f https://skupper.io/v2/install.yaml
      apply: readme
    - run: curl https://skupper.io/v2/install.yaml | sed s/Always/IfNotPresent/g |
        kubectl apply -f -
      apply: test
- title: Install the Skupper command-line tool
  numbered: true
  preamble: 'This example uses the Skupper command-line tool to create Skupper

    resources.  You need to install the `skupper` command only once

    for each development environment.


    On Linux or Mac, you can use the install script (inspect it

    [here][install-script]) to download and extract the command:


    ~~~ shell

    curl https://skupper.io/v2/install.sh | sh

    ~~~


    The script installs the command under your home directory.  It

    prompts you to add the command to your path if necessary.


    For Windows and other installation options, see [Installing

    Skupper][install-docs].


    [install-script]: https://github.com/skupperproject/skupper-website/blob/main/input/install.sh

    [install-docs]: https://skupper.io/#installation'
- title: Create your sites
  numbered: true
  preamble: 'A Skupper _site_ is a location where your application workloads

    are running.  Sites are linked together to form a network for your

    application.


    For each namespace, use `skupper site create` with a site name of

    your choice.  This creates the site resource and deploys the

    Skupper router to the namespace.


    **Note:** If you are using Minikube, you need to [start minikube

    tunnel][minikube-tunnel] before you run `skupper site create`.


    <!-- XXX Explain enabling link acesss on one of the sites -->


    [minikube-tunnel]: https://skupper.io/start/minikube.html#running-minikube-tunnel'
  postamble: 'You can use `skupper site status` at any time to check the status

    of your site.'
  commands:
    west:
    - run: skupper site create west --enable-link-access
      output: 'Waiting for status...

        Site "west" is configured. Check the status to see when it is ready

        '
    east:
    - run: skupper site create east
      output: 'Waiting for status...

        Site "east" is configured. Check the status to see when it is ready

        '
- commands:
    west:
    - run: skupper token issue ~/east-to-west.token
    east:
    - run: skupper token redeem ~/east-to-west.token
  title: Link your sites
  numbered: true
  preamble: 'A Skupper _link_ is a channel for communication between two sites.

    Links serve as a transport for application connections and

    requests.


    Creating a link requires the use of two Skupper commands in

    conjunction: `skupper token issue` and `skupper token redeem`.

    The `skupper token issue` command generates a secret token that

    can be transferred to a remote site and redeemed for a link to the

    issuing site.  The `skupper token redeem` command uses the token

    to create the link.


    **Note:** The link token is truly a *secret*.  Anyone who has the

    token can link to your site.  Make sure that only those you trust

    have access to it.


    First, use `skupper token issue` in west to generate the token.

    Then, use `skupper token redeem` in east to link the sites.'
  postamble: 'If your terminal sessions are on different machines, you may need

    to use `scp` or a similar tool to transfer the token securely.  By

    default, tokens expire after a single use or 15 minutes after

    being issued.'
- title: Deploy the iperf3 servers
  preamble: 'After creating the application router network, deploy `iperf3` in each
    namespace.

    '
  commands:
    east:
    - run: kubectl apply -f deployment-iperf3-a.yaml
    west:
    - run: kubectl apply -f deployment-iperf3-b.yaml
- title: Create connectors for the iperf3 servers
  preamble: 'With Skupper v2, connectors run in the namespace where the workload is
    deployed.

    Create a connector for each iperf3 server so the application network can reach
    the pods.

    '
  commands:
    east:
    - await_resource: deployment/iperf3-server-a
    - run: skupper connector create iperf3-server-a 5201 --workload deployment/iperf3-server-a
    west:
    - await_resource: deployment/iperf3-server-b
    - run: skupper connector create iperf3-server-b 5201 --workload deployment/iperf3-server-b
- title: Create listeners for the iperf3 services
  preamble: 'Listeners create service endpoints in each namespace so clients can reach
    those connectors.

    Configure listeners for every iperf3 service in each namespace.

    '
  commands:
    east:
    - run: skupper listener create iperf3-server-a 5201
    - run: skupper listener create iperf3-server-b 5201
    west:
    - run: skupper listener create iperf3-server-a 5201
    - run: skupper listener create iperf3-server-b 5201
- title: Run benchmark tests across the clusters
  preamble: "After deploying the iperf3 servers into the private and public cloud\
    \ clusters,\nthe virtual application network enables communications even though\
    \ they are \nrunning in separate clusters.\n"
  commands:
    east:
    - run: kubectl exec $(kubectl get pod -l application=iperf3-server-a -o=jsonpath='{.items[0].metadata.name}')
        -- iperf3 -c iperf3-server-a > results/east-to-a.log
    - run: kubectl exec $(kubectl get pod -l application=iperf3-server-a -o=jsonpath='{.items[0].metadata.name}')
        -- iperf3 -c iperf3-server-b > results/east-to-b.log
    west:
    - run: kubectl exec $(kubectl get pod -l application=iperf3-server-b -o=jsonpath='{.items[0].metadata.name}')
        -- iperf3 -c iperf3-server-a > results/west-to-a.log
    - run: kubectl exec $(kubectl get pod -l application=iperf3-server-b -o=jsonpath='{.items[0].metadata.name}')
        -- iperf3 -c iperf3-server-b > results/west-to-b.log
    - run: skupper debug dump
- commands:
    east:
    - run: kubectl delete deployment iperf3-server-a
    - run: skupper delete
    west:
    - run: kubectl delete deployment iperf3-server-b
    - run: skupper delete
  name: cleaning_up
  title: Cleaning up
  numbered: false
  preamble: 'To remove Skupper and the other resources from this exercise, use

    the following commands.'
next_steps: '- [Find more examples](https://skupper.io/examples/)

  '
